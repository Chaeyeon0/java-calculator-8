# java-calculator-precourse

### 🚀 미션 개요

사용자가 입력한 문자열 안의 숫자를 모두 더하는 프로그램

문자열 안에는 쉼표(`,`)나 콜론(`:`)을 기본 구분자로 사용하며, `//[구분자]\n` 형식으로 커스텀 구분자를 지정할 수도 있다.

---

### 🗂️ 패키지 구조

```
src/main/java/
└── calculator
    ├── Application.java             // 프로그램 실행 진입점
    ├── controller
    │   └── CalculatorController.java  // 전체 흐름 제어
    ├── view
    │   ├── InputView.java             // 사용자 입력 담당
    │   └── OutputView.java            // 결과 출력 담당
    └── domain
        ├── StringCalculator.java      // 계산기 핵심 로직 (파서 호출 및 합산)
        ├── ExpressionParser.java      // 문자열에서 구분자와 숫자 분리
        └── PositiveNumbers.java       // 숫자 유효성 검증 및 합산 수행

```

---

## 🧩 기능 요구사항

### 1️⃣ 기본 동작

- 사용자가 입력한 문자열을 분석하여 숫자를 모두 더한 결과를 반환한다.
- 예:
    ```
    입력: "1,2,3"
    결과: 6
    ```

### 2️⃣ 구분자 처리

- 기본 구분자: 쉼표(`,`), 콜론(`:`)
- 커스텀 구분자:
    - 문자열이 `"//"`로 시작하고, `\n` 전까지의 문자를 구분자로 사용한다.
    - 예:
        ```
        입력: "//;\n1;2;3"
        결과: 6
        ```
        
- 기능 목록:
    - [x]  기본 구분자(`,`, `:`) 이외의 커스텀 구분자가 있는지 확인
    - [x]  있다면 커스텀 구분자를 추출
    - [x]  구분자를 제외한 숫자 문자열 추출
    - [x]  문자열을 구분자로 split 처리


### 3️⃣ 유효성 검사

- 입력값이 비어 있으면 0을 반환한다.
- 구분자나 숫자에 잘못된 문자가 포함되면 예외를 발생시킨다.
- 음수 입력 시 예외를 발생시킨다.
- 기능 목록:
    - [x]  구분자가 숫자인지 확인
    - [x]  문자로 된 숫자가 실제 정수인지 확인
    - [x]  숫자가 아닌 문자가 있는 경우 예외 발생
    - [x]  음수인 경우 예외 발생


### 4️⃣ 계산 기능

- [x]  문자열을 입력받는다.
- [x]  구분자로 나누어진 숫자들을 추출한다.
- [x]  검증을 통과한 숫자들을 모두 더한다.
- [x]  결과를 출력한다.

---

### 구현 흐름

1. **`Application`**
    - 프로그램의 시작점으로, `CalculatorController`를 실행한다.
2. **`CalculatorController`**
    - 전체 흐름을 제어하는 역할을 담당한다.
    - 입력을 `InputView`에서 받아, `StringCalculator`에 전달하고
        
        계산 결과를 `OutputView`를 통해 출력한다.
        
3. **`InputView`**
    - 콘솔에서 문자열 입력을 받는다.
4. **`StringCalculator` (Domain)**
    - 프로그램의 핵심 로직을 담당한다.
    - 입력 문자열을 `ExpressionParser`로 넘겨 숫자 리스트를 받고,
        
        `PositiveNumbers`를 통해 유효성 검증 및 합산을 수행한다.
        
5. **`ExpressionParser` (Domain)**
    - 문자열을 분석하여
        - 커스텀 구분자가 있는지 확인하고,
        - 구분자에 따라 숫자 문자열을 분리한 뒤,
        - 숫자 리스트를 반환한다.
6. **`PositiveNumbers` (Domain)**
    - 숫자 리스트의 각 원소가 양수인지, 숫자인지 검증한다.
    - 유효하지 않은 값(음수, 문자)이 포함되어 있으면 예외를 발생시킨다.
    - 검증된 숫자들의 합계를 계산해 반환한다.
7. **`OutputView`**
    - 계산 결과 또는 예외 메시지를 콘솔에 출력한다.

---

### ✅ 목표

- 입력 문자열을 파싱하고, 숫자 유효성을 검사한 뒤 합계를 계산하는 과정을 객체지향적으로 설계
- 명확한 책임 분리를 통한 유지보수성 향상시키기
- 기능 단위의 커밋으로 Git 히스토리 관리
